{% extends 'backend/bare.html.twig' %}

{% block title %}{{ title }}{% endblock %}
    {% block content_bare%}
        <a href="{{ path('backend_activity') }}" class="btn btn-sm btn-outline-warning float-right"><i class="fas fa-backspace"></i> Cancel</a>
    <h1>Activity - add</h1>
    {{ form_start(activityForm) }}
    {{ form_row(activityForm.date) }}
    {{ form_row(activityForm.training) }}
    {{ form_row(activityForm.no_alcohol) }}
    {{ form_row(activityForm.no_candy) }}
    {{ form_row(activityForm.no_carbs) }}
    {{ form_row(activityForm.comment) }}
    <button type="submit" class="btn btn-sm btn-outline-primary">create</button>
    {{ form_end(activityForm) }}
{% endblock %}
